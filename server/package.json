{
  "name": "automovers-server",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "dev": "npx convex dev",
    "deploy": "npx convex deploy",
    "env:set": "npx convex env set"
  },
  "dependencies": {
    "convex": "^1.21.0",
    "browser-use-convex-component": "^0.1.0",
    "openai": "^4.80.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
