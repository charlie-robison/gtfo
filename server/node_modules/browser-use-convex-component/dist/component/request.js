import { BROWSER_USE_API_BASE, API_HEADER_KEY, } from "./constants.js";
export async function browserUseRequest(options) {
    const { method, path, apiKey, body } = options;
    const url = `${BROWSER_USE_API_BASE}${path}`;
    const headers = {
        [API_HEADER_KEY]: apiKey,
        "Content-Type": "application/json",
    };
    const response = await fetch(url, {
        method,
        headers,
        body: body ? JSON.stringify(body) : undefined,
    });
    if (!response.ok) {
        const text = await response.text();
        throw new Error(`Browser Use API error (${response.status}): ${text}`);
    }
    const text = await response.text();
    if (!text) {
        return undefined;
    }
    return JSON.parse(text);
}
//# sourceMappingURL=request.js.map